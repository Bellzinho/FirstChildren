<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.imc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ObesidadeIII"
            android:parentActivityName=".MainActivity"
            android:theme="@style/higKg2"
            >
        </activity>
        <activity android:name=".ObesidadeII"
            android:parentActivityName=".MainActivity"
            android:theme="@style/higKg1"
            />
        <activity android:name=".ObesidadeI"
            android:parentActivityName=".MainActivity"
            android:theme="@style/higKg"
            />
        <activity android:name=".SobrePeso"
            android:parentActivityName=".MainActivity"
            android:theme="@style/higKg"/>
        <activity android:name=".Abaixo"
            android:parentActivityName=".MainActivity"
            android:theme="@style/lowKg"/>
        <activity android:name=".Imc_peso_Ideal"
            android:parentActivityName=".MainActivity"
            android:theme="@style/ok"
            />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>